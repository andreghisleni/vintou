import styled from 'styled-components';

export const Team = styled.p`
  max-width: 900px;
  text-indent: 0;
  b {
    max-width: 900px;
    text-indent: 0;

    font-family: Roboto;
    font-weight: bold;
    font-size: 18px;
    line-height: 50px;

    padding: 10px;
  }

  ul {
    list-style: inside;
  }
`;
